export default function DashboardPage() {
  return (
    <div>
      <h1 className="font-display text-4xl text-jade-900">Dashboard</h1>
      <p className="mt-2 text-ink-500">A live look across every retreat.</p>
      <p className="mt-6 max-w-xl text-sm text-ink-400">
        Stats and recent activity arrive in the admin slice (Step 12).
      </p>
    </div>
  );
}
